"use client";

const Header = () => {
  return (
    <header className="header">
      <button onClick={() => console.log("You clicked")}>Sign In</button>
    </header>
  );
};

export default Header;
